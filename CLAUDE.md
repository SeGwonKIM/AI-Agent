# CLAUDE.md

`C:\AI-Agent` 는 김세권(GitHub `SeGwonKIM`)의 **학습·개발 작업 공간**이다.
하나의 프로그램이 아니라, 강의 실습물과 독립 프로젝트 여러 개가 한 폴더에 모여 있는 구조다.

응답과 커밋 메시지는 **한국어**로 쓴다.

## 폴더 지도

| 경로 | 내용 | git |
|---|---|---|
| `강의노트/` | 날짜별 강의 정리 (`{YYYY-MM-DD}/summary.md`) | 이 저장소 |
| `강의소스/` | **강의 원본 자료 (제3자 저작물)** | 커밋 금지 |
| `제출자료/` | 과제 제출본 (`drive_game`, `도매인역량`) | 이 저장소 |
| `todo_app/` | 5강 DB 실습 — 파이썬 내장 서버 + SQLite | 이 저장소 |
| `ksk_game/` `ksk_game1/` `bom_game/` | 단일 파일 HTML 게임 | **각자 독립 저장소** |
| `blog_ksk/` | Jekyll 블로그 (minimal-mistakes) | **독립 저장소** |
| `saju_tarot/` | 사주·타로 웹서비스 (FastAPI + Vite/TS) | **독립 저장소** (`SeGwonKIM/Saju_Talot`) |
| `New_project2/` `aiffel_test/` `kskFather_New_Project/` | 서브모듈 | **git submodule** |
| `0830_NewProject01/` `MyGithub0831/` `New_Project082901/` `PR_Demo_0831/` `Project_0831/` | 초기 Git/GitHub 실습 폴더 (README + figure) | 이 저장소 |

## 저장소 경계 — 제일 자주 실수하는 부분

- `C:\AI-Agent` 자체가 저장소(`SeGwonKIM/AI-Agent`)이고, **그 안에 별개 저장소들이 들어 있다.**
- 게임·블로그·사주타로 작업은 **반드시 해당 폴더 안에서** git 명령을 쓴다.
  루트에서 `git add -A` 를 하면 남의 저장소를 삼키거나 서브모듈 포인터를 망친다.
- 루트에서 `git status` 를 보면 독립 저장소들이 untracked 로 뜬다 — **정상이다. 커밋하지 않는다.**
- 서브모듈 3개는 내용을 직접 고치지 말고, 필요하면 원본 저장소에서 작업한 뒤 포인터만 갱신한다.

## 커밋하면 안 되는 것

`.gitignore` 에 걸어 뒀지만 이유를 알고 있어야 한다:

- `강의소스/`, `*.mhtml`, `강의노트/*/원본.md` — **모두의연구소 등 제3자 저작물.** 공개 저장소에 올리지 않는다.
  직접 정리한 `강의노트/*/summary.md` 만 커밋한다.
- `todo_app/todo.db` — 개인 할 일 목록. 이 PC에만 둔다.
- `saju_tarot` 의 `.env`, `backups/`, `현재주소.txt` — 비밀값·개인정보·임시 터널 주소.
- `blog_ksk/_site/` — 빌드 결과물.

## 환경

| | |
|---|---|
| OS | Windows 11 Pro |
| 셸 | PowerShell 기본 (Bash 툴도 사용 가능 — 문법이 다르니 섞지 않는다) |
| Python | 3.13.14 |
| 도구 | `gh` 2.98.0, `git` 2.55.0 |

### 한글 인코딩 — 반복해서 물린 함정

1. **파이썬을 Bash 툴에서 돌릴 때는 `PYTHONIOENCODING=utf-8` 을 붙인다.**
   안 붙이면 윈도우 콘솔 코드페이지 때문에 한글이 `5�� �����ϱ�` 처럼 깨진다.

   ```bash
   PYTHONIOENCODING=utf-8 python todo_app/app.py
   ```

2. **한글이 들어간 `.ps1` 파일은 UTF-8 **BOM**으로 저장한다.** BOM이 없으면 PowerShell이
   한글을 깨뜨린다 (`saju_tarot/상시실행.ps1` 에서 겪은 문제).

3. 파일을 새로 만들 때 한글 파일명은 피하고 영문 슬러그를 쓴다 (블로그 포스트는 특히).

## 작업별 스킬

이미 만들어 둔 것이 있으면 새로 짜지 말고 그것을 따른다.

| 하려는 일 | 쓸 것 |
|---|---|
| 새 HTML 게임/웹앱 만들어 GitHub Pages 배포 | `html-game-deploy` 스킬 |
| 강의 mhtml 에서 본문 뽑기 (요약 전 단계) | `lecture-extract` 스킬 |
| 강의노트 요약 | `/summarize-lecture` 커맨드 |
| 블로그에 정리 글 올리기 | `blog-post` 스킬 |
| Todo 앱 DB 조회 | `todo-db` 스킬 |

강의 정리는 위 세 개가 순서대로 이어지는 사슬이다:
`강의소스/{MMDD}/*.mhtml` → `원본.md` → `summary.md` → 블로그 글.
`강의소스/` 와 `원본.md` 는 제3자 저작물이라 커밋하지 않는다.

### 강의는 평일에만 열린다

**주말과 공휴일은 강의가 없다.** 폴더 이름을 +1 하다 주말을 건너뛰지 않으면 날짜가 어긋난다 —
`0904`(금) 다음 강의를 `0905` 로 저장한 일이 있었는데 그날은 토요일이었고 실제 강의는
`0907`(월)이었다. 요약과 블로그 글까지 만든 뒤 발견해 전부 되돌렸다.

그래서 **강의 날짜를 다룰 때는 추측하지 말고 요일을 확인한다.**

```bash
date -d 2026-09-07 '+%a'
```

공휴일은 `강의노트/휴일.txt` 에 한 줄씩 적어 둔다. `lecture-extract` 스크립트가 주말을
자동으로, 공휴일은 그 파일을 읽어 막아 준다.

## 코드 작성 취향

- **단일 파일 HTML 선호.** 게임·소도구는 `index.html` 하나에 CSS/JS를 인라인으로 넣고,
  외부 라이브러리와 빌드 도구를 쓰지 않는다. 파일을 열면 바로 실행돼야 한다.
- **PRD를 먼저 쓴다.** 기능이 조금이라도 복잡하면 `PRD.md` 를 작성하고 따로 커밋한 뒤 구현한다.
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 제목에 넣는다.
  좋음: `feat: 수류탄이 터졌을 때 직전 상태로 되돌리는 버튼 추가` / 나쁨: `update`, `fix bug`
- 설명은 **비전공자 눈높이**로 한다. 비유를 먼저 주고 용어를 나중에 붙인다.

## saju_tarot 은 규칙이 다르다

가장 큰 프로젝트이고 다른 폴더와 성격이 다르다.

- 스택: FastAPI + uvicorn (backend) / Vite + TypeScript (frontend) / Docker + Render 배포.
- **`saju_tarot/docs/PRD.md` 가 진짜 명세다.** 코드 주석이 `PRD §12.16` 처럼 조항을 참조한다.
  구현을 바꾸기 전에 해당 조항을 먼저 읽는다.
- `backend/requirements.txt` 는 **버전을 고정**하고, 취약점 때문에 올린 이유를 주석으로 남겨 뒀다.
  패키지를 추가할 때는 실존 여부와 다운로드 수를 확인하고 주석에 근거를 적는다.
- **유료 전환을 계획하고 있다** (과제 기한 이후). 그래서 새 의존성은 **상업적 이용이 가능한 라이선스**만
  쓴다. GPL 계열이나 비상업 전용 라이브러리를 넣지 않는다.
- 만세력 계산에 함정이 있다: `korean-lunar-calendar` 는 없는 윤달에도 True를 반환해 왕복 검증이 필요하고,
  `lunar-python` 의 절기는 베이징 기준이라 `app/korea_time.py` 보정을 반드시 거친다.
- `.ps1` 스크립트(`서버_실행.ps1`, `상시실행.ps1`, `백업.ps1`)로 로컬 서버·터널·백업을 돌린다.
  실행 방법은 각 파일 상단 주석에 있다.

## 하지 말 것

- 확인 없이 `git push` 하거나 공개 저장소에 올리지 않는다 — 게임과 블로그는 **공개**다.
- 블로그 글·README·PRD에 개인정보, 이메일, 로컬 절대경로, `.env` 값을 넣지 않는다.
- 자율 실행 모드(`/autopilot`, `/ralph`)를 **작업 트리가 더러운 상태에서** 돌리지 않는다.
  루트에 untracked 폴더가 여러 개 있어 되돌리기가 어렵다. 커밋을 먼저 정리한다.
- `todo.db` 에 대한 쓰기(`INSERT`/`UPDATE`/`DELETE`)는 명확히 요청받았을 때만 하고,
  무엇을 바꾸는지 먼저 말한다.
